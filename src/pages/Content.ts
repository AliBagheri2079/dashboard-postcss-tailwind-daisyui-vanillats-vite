//* Rout Management
const Content = document.createElement("main");
Content.setAttribute(
  "class",
  "rounded-b-box relative col-start-2 col-end-[-1] row-span-2 bg-base-100"
);

// Content.appendChild(BreadCrumbs);
Content.innerHTML = `
  <section>
    <!--
      web components
      crypto with table and placeholder,
      chat, 
      todo with firebase, 
      contentfull or sanity.io
      login form (auth & regex) wiht upload photo, 
      spa, 
      three.js
    -->
    Content
  </section>
`;

export default Content;
